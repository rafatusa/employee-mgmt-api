<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    Spring Boot 3.5.x is the current supported 3.x line. The project was
    originally pinned to 3.2.11, but 3.2.x left OSS support in 2024 and its
    dependency tree carries fixable CRITICAL CVEs that the Trivy image gate
    correctly refused to ship. Upgrading fixes those at the source rather than
    suppressing the finding.
  -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.udap</groupId>
  <artifactId>employee-mgmt-api</artifactId>
  <version>1.0.0</version>
  <name>employee-mgmt-api</name>
  <description>Employee Management Web API - Spring Boot 3.5 / Java 21</description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- springdoc 2.8.x targets Spring Boot 3.x; the 3.x line targets Boot 4. -->
    <springdoc.version>2.8.17</springdoc.version>
    <jjwt.version>0.12.7</jjwt.version>
    <jacoco.version>0.8.12</jacoco.version>
    <checkstyle.plugin.version>3.3.1</checkstyle.plugin.version>
    <pmd.plugin.version>3.21.2</pmd.plugin.version>
    <spotbugs.plugin.version>4.8.3.1</spotbugs.plugin.version>
    <dependency.check.version>9.2.0</dependency.check.version>

    <!--
      KNOWN OPEN VULNERABILITY - tomcat-embed-core (see docs/security-notes.md).

      Spring Boot 3.5.16 manages tomcat-embed-core 10.1.55. The container scan
      reports three fixable CRITICAL vulnerabilities in it:

        CVE-2026-65182  security constraint bypass (improper access control)
        CVE-2026-65905  authentication bypass via replay in the DIGEST authenticator
        CVE-2026-68525  unauthorized resource access via FORM authentication bypass

      All three are fixed in Tomcat 10.1.58. That version is TAGGED in the Apache
      Tomcat repository but is NOT YET PUBLISHED to Maven Central, so overriding
      `tomcat.version` to 10.1.58 fails dependency resolution:
        Could not find artifact org.apache.tomcat.embed:tomcat-embed-core:jar:10.1.58

      The override is therefore left OUT rather than pinned to a version that
      does not resolve. As soon as 10.1.58 lands in Central (or Spring Boot
      publishes a release that manages >= 10.1.58), set:

        <tomcat.version>10.1.58</tomcat.version>

      and re-run build-deploy; the trivy-report artifact will confirm the image
      is clean. Do NOT silence this finding with a .trivyignore.
    -->

    <!--
      The NVD API rejects unauthenticated bulk downloads (HTTP 403/404), so the
      dependency-check gate needs a key. CI supplies it from the NVD_API_KEY
      repository secret via -Dnvd.api.key=...; the empty default keeps local
      `mvn verify` working, because dependency-check only runs when explicitly
      invoked, never as part of the default lifecycle.
    -->
    <nvd.api.key></nvd.api.key>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>check-coverage</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.90</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
              <excludes>
                <exclude>com/udap/employee/EmployeeMgmtApiApplication.class</exclude>
                <exclude>com/udap/employee/config/**</exclude>
                <exclude>com/udap/employee/dto/**</exclude>
              </excludes>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle.plugin.version}</version>
        <configuration>
          <configLocation>config/checkstyle/checkstyle.xml</configLocation>
          <consoleOutput>true</consoleOutput>
          <failsOnError>true</failsOnError>
          <violationSeverity>error</violationSeverity>
          <includeTestSourceDirectory>false</includeTestSourceDirectory>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${pmd.plugin.version}</version>
        <configuration>
          <rulesets>
            <ruleset>config/pmd/ruleset.xml</ruleset>
          </rulesets>
          <printFailingErrors>true</printFailingErrors>
          <failOnViolation>true</failOnViolation>
          <includeTests>false</includeTests>
        </configuration>
      </plugin>

      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.plugin.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>High</threshold>
          <failOnError>true</failOnError>
          <excludeFilterFile>config/spotbugs/exclude.xml</excludeFilterFile>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${dependency.check.version}</version>
        <configuration>
          <failBuildOnCVSS>9</failBuildOnCVSS>
          <skipTestScope>true</skipTestScope>
          <nvdApiKey>${nvd.api.key}</nvdApiKey>
          <nvdApiDelay>8000</nvdApiDelay>
          <nvdMaxRetryCount>10</nvdMaxRetryCount>
          <suppressionFiles>
            <suppressionFile>config/dependency-check/suppressions.xml</suppressionFile>
          </suppressionFiles>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
