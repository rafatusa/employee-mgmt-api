# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Checkstyle
      run: mvn -B -ntp checkstyle:check
    - name: PMD
      run: mvn -B -ntp pmd:check
    - name: SpotBugs
      run: mvn -B -ntp compile spotbugs:check
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Unit tests with JaCoCo coverage gate
      run: mvn -B -ntp verify
  security:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Semgrep SAST
      uses: semgrep/semgrep-action@v1
      with:
        config: p/java p/secrets
  build_push:
    runs-on: ubuntu-latest
    needs:
    - test
    - security
    env:
      IMAGE: ghcr.io/${{ github.repository }}
      TRIVY_VERSION: v0.74.0
    timeout-minutes: 35
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Package application
      run: mvn -B -ntp -DskipTests package
    - name: Log in to GHCR
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor
        }} --password-stdin
    - name: Build image
      run: 'IMAGE_LC="$(echo "$IMAGE" | tr ''[:upper:]'' ''[:lower:]'')"

        docker build -t "$IMAGE_LC:${{ github.sha }}" -t "$IMAGE_LC:latest" .

        '
    - name: Install Trivy
      run: "curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh\
        \ \\\n  | sh -s -- -b /usr/local/bin \"$TRIVY_VERSION\"\ntrivy --version\n"
    - name: Trivy scan report
      run: "mkdir -p reports\nIMAGE_LC=\"$(echo \"$IMAGE\" | tr '[:upper:]' '[:lower:]')\"\
        \ntrivy image --severity CRITICAL --ignore-unfixed --format table \\\n  --output\
        \ reports/trivy-critical.txt \"$IMAGE_LC:${{ github.sha }}\"\ntrivy image\
        \ --severity CRITICAL --ignore-unfixed --format json \\\n  --output reports/trivy-critical.json\
        \ \"$IMAGE_LC:${{ github.sha }}\"\necho \"### Trivy CRITICAL (fixable) findings\"\
        \ >> \"$GITHUB_STEP_SUMMARY\"\necho '```' >> \"$GITHUB_STEP_SUMMARY\"\ncat\
        \ reports/trivy-critical.txt >> \"$GITHUB_STEP_SUMMARY\"\necho '```' >> \"\
        $GITHUB_STEP_SUMMARY\"\ncat reports/trivy-critical.txt\n"
    - name: Publish Trivy report
      uses: actions/upload-artifact@v4
      with:
        name: trivy-report
        path: reports
    - name: Trivy gate (fails on fixable CRITICAL)
      run: 'IMAGE_LC="$(echo "$IMAGE" | tr ''[:upper:]'' ''[:lower:]'')"

        trivy image --exit-code 1 --severity CRITICAL --ignore-unfixed --quiet "$IMAGE_LC:${{
        github.sha }}"

        '
    - name: Push image
      run: 'IMAGE_LC="$(echo "$IMAGE" | tr ''[:upper:]'' ''[:lower:]'')"

        docker push "$IMAGE_LC:${{ github.sha }}"

        docker push "$IMAGE_LC:latest"

        '
  provision:
    runs-on: ubuntu-latest
    needs:
    - build_push
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform format check
      run: terraform fmt -check -recursive
      working-directory: infra
    - name: Terraform validate
      run: terraform validate
      working-directory: infra
    - name: Terraform plan
      run: terraform plan -input=false -out=tfplan
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false -auto-approve tfplan
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - id: tf
      name: Read infrastructure outputs
      run: 'echo "host=$(terraform output -raw public_ip)" >> "$GITHUB_OUTPUT"

        terraform output -raw db_endpoint > /tmp/db_endpoint

        terraform output -raw db_name > /tmp/db_name

        terraform output -raw db_username > /tmp/db_username

        '
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        HOST: ${{ steps.tf.outputs.host }}
    - name: Render Puppet hieradata
      run: bash puppet/render_hieradata.sh
      env:
        IMAGE_REF: ghcr.io/${{ github.repository }}
        IMAGE_TAG: ${{ github.sha }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
    - name: Copy Puppet code to instance
      run: scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new -r puppet
        "$SSH_USER@$HOST:/tmp/puppet"
      env:
        HOST: ${{ steps.tf.outputs.host }}
        SSH_USER: ${{ secrets.SSH_USER }}
    - name: Install Puppet agent
      run: ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new "$SSH_USER@$HOST"
        'bash /tmp/puppet/bootstrap.sh'
      env:
        HOST: ${{ steps.tf.outputs.host }}
        SSH_USER: ${{ secrets.SSH_USER }}
    - name: Puppet apply (Docker, Nginx, systemd, container)
      run: ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new "$SSH_USER@$HOST"
        'sudo /opt/puppetlabs/bin/puppet apply --detailed-exitcodes --modulepath=/tmp/puppet/modules
        --hiera_config=/tmp/puppet/hiera.yaml /tmp/puppet/manifests/site.pp; rc=$?;
        test $rc -eq 0 -o $rc -eq 2'
      env:
        HOST: ${{ steps.tf.outputs.host }}
        SSH_USER: ${{ secrets.SSH_USER }}
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Health check through Nginx
      run: 'HOST="$(terraform output -raw public_ip)"

        curl --fail --retry 20 --retry-delay 15 --retry-all-errors "http://$HOST/actuator/health"

        '
      working-directory: infra
    - name: Verify API documentation is published
      run: 'HOST="$(terraform output -raw public_ip)"

        curl --fail --retry 5 --retry-delay 10 --retry-all-errors "http://$HOST/v3/api-docs"

        '
      working-directory: infra
